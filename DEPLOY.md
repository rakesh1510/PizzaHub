# Deploy / Upload PizzaHub to GitHub

If your GitHub repo currently only shows `.gitkeep`, push this local branch to `main`.

```bash
git remote -v
# if needed:
# git remote add origin https://github.com/<your-username>/PizzaHub.git

git branch -M main
git push -u origin main
```

Then refresh GitHub.
